const CORE_CACHE='flight-universe-core-v8-navigation';
const ASSET_CACHE='flight-universe-assets-v6';
const CORE=["./","./index.html","./styles.css","./bootstrap.js","./app-part-00.txt","./app-part-01.txt","./app-part-02.txt","./app-part-03.txt","./app-part-04.txt","./app-part-05.txt","./manifest.webmanifest","./icon.svg"];
const CACHEABLE_EXTERNAL=new Set(['cdn.jsdelivr.net','raw.githubusercontent.com','assets.science.nasa.gov','threejs.org']);
self.addEventListener('install',event=>{event.waitUntil(caches.open(CORE_CACHE).then(cache=>cache.addAll(CORE)).then(()=>self.skipWaiting()));});
self.addEventListener('activate',event=>{event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>![CORE_CACHE,ASSET_CACHE].includes(k)).map(k=>caches.delete(k)))).then(()=>self.clients.claim()));});
self.addEventListener('fetch',event=>{if(event.request.method!=='GET')return;const url=new URL(event.request.url);const sameOrigin=url.origin===self.location.origin;const cacheExternal=CACHEABLE_EXTERNAL.has(url.hostname);if(!sameOrigin&&!cacheExternal)return;event.respondWith(caches.match(event.request).then(hit=>{if(hit)return hit;return fetch(event.request).then(response=>{if(response&&(response.ok||response.type==='opaque')){const copy=response.clone();caches.open(sameOrigin?CORE_CACHE:ASSET_CACHE).then(cache=>cache.put(event.request,copy)).catch(()=>{});}return response;}).catch(async()=>{if(event.request.mode==='navigate')return(await caches.match('./index.html'))||Response.error();return Response.error();});}));});
